services:
  postgres:
    container_name: txai-postgres-pedrodecf
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: txai
      POSTGRES_PASSWORD: txai
      POSTGRES_DB: txai